apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
  - ../../base
patchesStrategicMerge:
  - custom-image-dc.yaml
configurations:
  - params.yaml
configMapGenerator:
  - name: odh-dashboard-config
    envs:
      - params.env
vars:
  - name: ODH_DASHBOARD_IMAGE
    objref:
      name: odh-dashboard-config
      kind: ConfigMap
      apiVersion: v1
    fieldref:
      fieldpath: data.ODH_DASHBOARD_IMAGE